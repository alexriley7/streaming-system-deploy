apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer-profile
spec:
  replicas: 1 #switch to 0 to stop producer
  selector:
    matchLabels:
      app: producer-profile
  template:
    metadata:
      labels:
        app: producer-profile
    spec:
      containers:
        - name: profile-producer
          image: ghcr.io/alexriley7/streaming-system/producer-profile:dc81b59bdea521a0c85756b3895bada1a420c5ed
          imagePullPolicy: Always

          # Added: 

          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: my-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092

            - name: ENABLE_PRODUCER
              value: "false"   # switch to "false" to stop producing

            - name: TOPIC
              value: profiles-input-topic-debug-a4


      imagePullSecrets:
        - name: ghcr-secret